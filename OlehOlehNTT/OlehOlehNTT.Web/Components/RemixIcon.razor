<i @attributes="AdditionalAttributes" class="@CssClass()" style="font-size:@(Size)rem"></i>

@code {
    [Parameter]
    public string Icon { get; set; } = "";

    [Parameter]
    public IconType Type { get; set; }

    [Parameter]
    public double Size { get; set; } = 1;

    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private string IconTypeToString() =>
        Type switch
        {
            IconType.Line => "line",
            IconType.Fill => "fill",
            _ => "line"
        };

    private string CssClass() => $"ri-{Icon}-{IconTypeToString()}";
}